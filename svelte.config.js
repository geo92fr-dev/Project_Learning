// Configuration Svelte simplifiée pour template
/** @type {import('@sveltejs/kit').Config} */
const config = {
  kit: {
    // Configuration minimale - l'adapter sera installé dans le projet final
    adapter: undefined
  }
};

export default config;
